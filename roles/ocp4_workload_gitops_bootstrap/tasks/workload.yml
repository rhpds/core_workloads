---
# Do not change this task.  It supports multiple configs.
# If you need to change variable names, do so in your AgV.
- name: Set _ocp4_workload_gitops_bootstrap_deployer_values
  ansible.builtin.set_fact:
    _ocp4_workload_gitops_bootstrap_deployer_values:
      deployer:
        domain: "{{ openshift_cluster_ingress_domain }}"
        apiUrl: "{{ openshift_api_url }}"
        guid: "{{ guid }}"

- name: Debug _ocp4_workload_gitops_bootstrap_deployer_values
  ansible.builtin.debug:
    msg: "{{ _ocp4_workload_gitops_bootstrap_deployer_values | to_yaml }}"

- name: Check if ArgoCD application already exists
  kubernetes.core.k8s_info:
    api_version: argoproj.io/v1alpha1
    kind: Application
    name: "{{ ocp4_workload_gitops_bootstrap_application_name }}"
    namespace: "{{ ocp4_workload_gitops_bootstrap_namespace }}"
  register: _existing_application

- name: Fail if ArgoCD application already exists
  ansible.builtin.fail:
    msg: >-
      ArgoCD Application '{{ ocp4_workload_gitops_bootstrap_application_name }}'
      already exists in namespace '{{ ocp4_workload_gitops_bootstrap_namespace }}').
      If you are running a tenant application, set a unique name.
  when: _existing_application.resources | length > 0

- name: Ensure ArgoCD AppProject exists
  when: ocp4_workload_gitops_bootstrap_application_project != 'default'
  block:
    - name: Check if ArgoCD AppProject exists
      kubernetes.core.k8s_info:
        api_version: argoproj.io/v1alpha1
        kind: AppProject
        name: "{{ ocp4_workload_gitops_bootstrap_application_project }}"
        namespace: openshift-gitops
      register: _existing_appproject

    - name: Create ArgoCD AppProject
      when: _existing_appproject.resources | length == 0
      kubernetes.core.k8s:
        state: present
        template: appproject.yaml.j2

- name: Create bootstrap ArgoCD application
  kubernetes.core.k8s:
    state: present
    template: application.yaml.j2

# Bootstrap Application created has no labels. Consider adding.
#
# Child applications are tracked:
#
# creates tracking annotations like this:
#   annotations:
#     argocd.argoproj.io/tracking-id: 'infra-bootstrap:argoproj.io/Application:openshift-gitops/bastion'
#
#   annotations:
#     argocd.argoproj.io/tracking-id: 'bootstrap:argoproj.io/Application:openshift-gitops/infra-bootstrap'

- name: Trigger immediate sync on bootstrap ArgoCD application
  kubernetes.core.k8s:
    api_version: argoproj.io/v1alpha1
    kind: Application
    name: "{{ ocp4_workload_gitops_bootstrap_final_application_name }}"
    namespace: "{{ ocp4_workload_gitops_bootstrap_namespace }}"
    merge_type: merge
    definition:
      operation:
        initiatedBy:
          username: admin
        sync:
          syncStrategy:
            hook: {}

- name: Wait until bootstrap ArgoCD application is healthy and synced
  kubernetes.core.k8s_info:
    api_version: argoproj.io/v1alpha1
    kind: Application
    name: "{{ ocp4_workload_gitops_bootstrap_application_name }}"
    namespace: openshift-gitops
  register: argocd_bootstrap
  retries: "{{ ocp4_workload_gitops_bootstrap_health_retries }}"
  delay: 10
  until:
    - argocd_bootstrap is defined
    - argocd_bootstrap.resources is defined
    - argocd_bootstrap.resources | length > 0
    - argocd_bootstrap.resources[0].status is defined
    - argocd_bootstrap.resources[0].status.health.status is defined
    - argocd_bootstrap.resources[0].status.health.status == "Healthy"
    - argocd_bootstrap.resources[0].status.sync.status is defined
    - argocd_bootstrap.resources[0].status.sync.status == "Synced"
  ignore_errors: "{{ ocp4_workload_gitops_bootstrap_health_ignore | bool }}"

- name: Pause for a minute while the bootstrap app deploys
  ansible.builtin.pause:
    seconds: 60

- name: Validate all applications with our label in any namespace are healthy and synced
  when: ocp4_workload_gitops_bootstrap_application_wait | bool
  kubernetes.core.k8s_info:
    api_version: argoproj.io/v1alpha1
    kind: Application
    label_selectors:
      - "app.kubernetes.io/instance={{ ocp4_workload_gitops_bootstrap_application_name }}"
  register: _all_apps
  delay: 10
  retries: "{{ ocp4_workload_gitops_bootstrap_application_health_retries }}"
  until:
    - _all_apps is defined
    - _all_apps.resources is defined
    # json_query: length of list of resources found == length of list of those with good status
    - _all_apps.resources | json_query('length([?status.health.status==`Healthy` || status.health.status==`Suspended`]) == length(@)')
    - _all_apps.resources | json_query('length([?status.sync.status==`Synced`]) == length(@)')

#######
####### Begin processing GitOps output
#######

# This role will MERGE all the relevant provision_data from all matching configmaps.
#
# A TENANT provision should only care about the user_data relevant to that tenant.
# If there is more than one tenant, do not merge the user_data of the two tenants.
# bootstrap-platform is a special case, handled later.
#
# Expected ConfigMap format for TENANT data:
# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: tenant-<GUID>-<SUBJECT>-provisiondata
#   namespace: <ANY NAMESPACE>
#   labels:
#     demo.redhat.com/tenant-<GUID>: "true"
#   annotations:
#     argocd.argoproj.io/sync-options: "Prune=true"
# data:
#   provision_data: |
#     <top level provision data>
#     users:
#       <USERNAME>:
#         exampleVar: exampleValue
#
# Expected ConfigMap format for INFRA data:
# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: infra-<SUBJECT>-provisiondata
#   namespace: <ANY NAMESPACE>
#   labels:
#     demo.redhat.com/infra-<GUID>
#   annotations:
#     argocd.argoproj.io/sync-options: "Prune=true"
# data:
#   provision_data: |
#     <top level provision data>
#     users: # if created
#       <USERNAME>:
#         namespace: {{ $nsName }}

# - name: Set the configmap label variable
#   ansible.builtin.set_fact:
#     configmap_label: >-
#       {{ 'demo.redhat.com/tenant-' ~ guid
#          if ocp4_workload_gitops_bootstrap_application_name == 'bootstrap-tenant'
#          else 'demo.redhat.com/infra' }}

# - name: Retrieve matching ConfigMaps and wait for provision_data, if any
#   kubernetes.core.k8s_info:
#     api_version: v1
#     kind: ConfigMap
#     label_selectors:
#       - "{{ configmap_label }}"
#   register: cm_provision_data
#   retries: 30
#   delay: 10
#   until:
#     - cm_provision_data.resources | length > 0
#     - >-
#       cm_provision_data.resources | map(attribute='data')
#       | map(attribute='provision_data', default='')
#       | select('ne', '') | list | length == cm_provision_data.resources | length
#   failed_when: false

# - name: Process ConfigMap provision data, if any
#   when:
#     - cm_provision_data.resources | default([]) | length > 0
#   block:
#     - name: Merge provision_data from all ConfigMaps
#       ansible.builtin.set_fact:
#         provision_data: "{{ provision_data | default({}) | combine(item.data.provision_data | from_yaml, recursive=True) }}"
#       loop: "{{ cm_provision_data.resources }}"
#       when: item.data.provision_data is defined

#     - name: Add data to agnosticd_user_info
#       agnosticd.core.agnosticd_user_info:
#         data: "{{ provision_data }}"
